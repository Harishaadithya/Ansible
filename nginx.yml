---
- hosts: ubuntu
  become: yes
  gather_facts: true

  tasks:
    - name: Installing Nginx
      apt:
        name: nginx
        state: present

    - name: Download web template
      shell: cd /home/ubuntu/ && wget -o harish.zip https://www.tooplate.com/download/2114_pixie

    - name: unzip
      unarchive:
          src: /home/ubuntu/harish.zip
          dest: /home/ubuntu/
    
    - name: copy files to nginx html directory
      copy: 
        src: /home/ubuntu/harish/*
        dest: /usr/share/nginx/html/
      notify: restart nginx


  handlers:
    - name: restart nginx
      service:
        name: nginx
        state: restarted
